suite: crd test
tests:
  - it: can disable CRDs
    set:
      proxy.installCRDs: false
    template: crds/ingress-route.yaml
    asserts:
      - hasDocuments:
          count: 0
  - it: enable CRDs by default
    template: crds/ingress-route.yaml
    asserts:
      - isKind:
          of: CustomResourceDefinition
      - isAPIVersion:
          of: apiextensions.k8s.io/v1
      - equal:
          path: metadata.name
          value: ingressroutes.traefik.containo.us
      - hasDocuments:
          count: 1
