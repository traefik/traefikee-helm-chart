{{ $token := randAlphaNum 10 | nospace | b64enc}}

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.cluster }}-controller
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "common.labels" . | indent 4 }}
spec:
  selector:
    matchLabels:
      app: traefikee
      release: {{ .Values.cluster }}
      component: controllers
  podManagementPolicy: Parallel
  serviceName: {{ .Values.cluster }}-ctrl-svc
  replicas: {{ .Values.controller.controllerSpecs.replicas }}
  template:
    metadata:
      labels:
        app: traefikee
        release: {{ .Values.cluster }}
        component: controllers
    spec:
      serviceAccountName: {{ .Values.cluster }}-svc-acc
{{- if or .Values.controller.controllerSpecs.affinity .Values.controller.controllerSpecs.podAntiAffinity }}
      affinity:
{{- if .Values.controller.controllerSpecs.affinity }}
{{ toYaml .Values.controller.controllerSpecs.affinity | indent 8 }}
{{- end }}
        podAntiAffinity:
{{- if .Values.controller.controllerSpecs.podAntiAffinity }}
{{ toYaml .Values.controller.controllerSpecs.podAntiAffinity | indent 10 }}
{{- end }}
{{- end }}
{{- if .Values.controller.controllerSpecs.tolerations }}
      tolerations:
{{ toYaml .Values.controller.controllerSpecs.tolerations | indent 8 }}
{{- end }}
      terminationGracePeriodSeconds: 30
      initContainers:
        - name: wait-dns
          image: busybox:1.31.1
          command: ['sh', '-c', 'until nslookup -type=a {{ printf "%s-ctrl-svc.%s" .Values.cluster .Release.Namespace }}.svc.cluster.local; do echo waiting for published dns records; sleep 1; done;']
          resources:
            requests:
              memory: "10Mi"
              cpu: "100m"
            limits:
              memory: "100Mi"
              cpu: "1000m"
      containers:
        - name: {{ .Values.cluster }}-controller
          image: {{ template "traefikee-helm-chart.image-name" . }}
          imagePullPolicy: {{ .Values.controller.controllerSpecs.containers.containerSpecs.controller.pullPolicy }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: LICENSE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.cluster }}-license
                  key: license
          securityContext:
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
          ports:
            - containerPort: 4242
              name: control-port
            - containerPort: 8080
              name: api
          readinessProbe:
            tcpSocket:
              port: control-port
            initialDelaySeconds: 10
            periodSeconds: 5
{{- if .Values.controller.controllerSpecs.containers.containerSpecs.controller.resources }}
          resources:
{{ toYaml .Values.controller.controllerSpecs.containers.containerSpecs.controller.resources | indent 12 }}
{{- end }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/traefikee
            - name: sockets
              mountPath: /var/run/traefikee
            {{ if (.Values.controller.controllerSpecs.containers.containerSpecs.controller.staticConfig).configMap }}
            - name: staticconfig
              mountPath: /var/run/traefikee/config
            {{ end }}
          command:
            - "/traefikee"
            - "controller"
            - "--name=$(POD_NAME)"
            - "--advertise=$(POD_NAME).{{ .Values.cluster }}-ctrl-svc.$(POD_NAMESPACE):4242"
            - "--discovery.dns.bootstrapexpected={{ .Values.controller.controllerSpecs.replicas }}"
            - "--discovery.dns.domain={{ .Values.cluster }}-ctrl-svc.$(POD_NAMESPACE)"
            - "--jointoken.kubernetes.prefix={{ .Values.cluster }}"
            - "--jointoken.kubernetes.namespace={{ .Release.Namespace }}"
            - "--license=$(LICENSE)"
            - "--api.autocerts"
            {{ if (.Values.controller.controllerSpecs.containers.containerSpecs.controller.staticConfig).configMap }}
            - "--configFile=/var/run/traefikee/config/{{ .Values.controller.controllerSpecs.containers.containerSpecs.controller.staticConfig.configMapKey }}"
            {{ end }}
            - "--log.level="
            - "--log.filepath="
            - "--log.format="
            - "--plugin.url=https://{{ .Values.cluster }}-plugin-registry-svc.{{ .Release.Namespace }}.svc.cluster.local"
            - "--plugin.token={{ $token }}"
      volumes:
        - name: sockets
          emptyDir: {}
        {{ if (.Values.controller.controllerSpecs.containers.containerSpecs.controller.staticConfig).configMap }}
        - name: staticconfig
          configMap:
            name: {{ .Values.controller.controllerSpecs.containers.containerSpecs.controller.staticConfig.configMap }}
        {{ end }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.cluster }}-plugin-registry
  namespace: {{ .Release.Namespace }}
  labels:
    app: traefikee
    release: {{ .Values.cluster }}
spec:
  serviceName: {{ .Values.cluster }}-plugin-registry-svc
  replicas: {{ .Values.registry.registrySpecs.replicas }}
  selector:
    matchLabels:
      app: traefikee
      release: {{ .Values.cluster }}
      component: registry
  template:
    metadata:
      labels:
        app: traefikee
        release: {{ .Values.cluster }}
        component: registry
    spec:
{{- if or .Values.registry.registrySpecs.affinity .Values.registry.registrySpecs.podAntiAffinity }}
      affinity:
{{- if .Values.registry.registrySpecs.affinity }}
{{ toYaml .Values.registry.registrySpecs.affinity | indent 8 }}
{{- end }}
        podAntiAffinity:
{{- if .Values.registry.registrySpecs.podAntiAffinity }}
{{ toYaml .Values.registry.registrySpecs.podAntiAffinity | indent 10 }}
{{- end }}
{{- end }}
{{- if .Values.registry.registrySpecs.tolerations }}
      tolerations:
{{ toYaml .Values.registry.registrySpecs.tolerations | indent 8 }}
{{- end }}
      terminationGracePeriodSeconds: 30
      automountServiceAccountToken: false
      initContainers:
        - name: wait-dns
          image: busybox:1.31.1
          command: ['sh', '-c', 'until nslookup -type=a {{ printf "%s-ctrl-svc.%s" .Values.cluster .Release.Namespace }}.svc.cluster.local; do echo waiting for published dns records; sleep 1; done;']
          resources:
            requests:
              memory: "10Mi"
              cpu: "100m"
            limits:
              memory: "100Mi"
              cpu: "1000m"
      containers:
        - name: {{ .Values.cluster }}-controller
          image: {{ template "traefikee-helm-chart.image-name" . }}
          imagePullPolicy: {{ .Values.registry.registrySpecs.containers.containerSpecs.registry.pullPolicy }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          securityContext:
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
          ports:
            - containerPort: 80
              name: http
            - containerPort: 443
              name: https
          # readinessProbe:
          #   tcpSocket:
          #     port: http
          #   initialDelaySeconds: 2
          #   periodSeconds: 5
{{- if .Values.registry.registrySpecs.containers.containerSpecs.registry.resources }}
          resources:
{{ toYaml .Values.registry.registrySpecs.containers.containerSpecs.registry.resources | indent 12 }}
{{- end }}
          volumeMounts:
            - name: {{ .Values.cluster }}-plugin-registry-data
              mountPath: "/var/lib/traefikee"
            - name: plugin-data
              mountPath: "/var/lib/plugins"
            - name: "join-token"
              mountPath: "/var/run/secrets"
          command:
            - "/traefikee"
            - "plugin-registry"
            - "--name=$(POD_NAME)"
            - "--plugindir=/var/lib/plugins"
            - "--token={{ $token }}"
            - "--discovery.dns.domain={{ .Values.cluster }}-ctrl-svc.$(POD_NAMESPACE)"
            - "--jointoken.file.path=/var/run/secrets"
            - "--log.level="
            - "--log.filepath="
            - "--log.format="
      volumes:
        - name: {{ .Values.cluster }}-plugin-registry-data
          emptyDir: {}
        - name: "join-token"
          secret:
            secretName: {{ .Values.cluster }}-tokens
  volumeClaimTemplates:
    - metadata:
        name: plugin-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
